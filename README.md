## 🚀 시작 가이드 (Getting Started)
1. 저장소 코드를 클론 또는 풀(Pull) 받습니다.
   `git pull origin main` (또는 본인 브랜치)
2. `config/` 폴더 아래에 `.env.example` 파일을 복사하여 `.env` 파일을 생성합니다.
3. 생성한 `.env` 파일 안에서 본인의 환경에 맞는 DB 접속 정보(`DB_HOST`, `DB_PASSWORD` 등)를 입력해주세요!